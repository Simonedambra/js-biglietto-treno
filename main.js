'use strict'

const chilometri = Number(prompt('inserisci km'));
const eta = Number(prompt('inserisci l\'età'));
const prezzokm = Number(0.21)
let prezzo = Number(chilometri * prezzokm)


console.log(prezzo);
